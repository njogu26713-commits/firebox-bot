<svg width="100%" height="120" viewBox="0 0 800 120" fill="none" xmlns="http://www.w3.org/2000/svg">
  <defs>
    <linearGradient id="glowGrad" x1="0%" y1="0%" x2="100%" y2="0%">
      <stop offset="0%" stop-color="#FF6A00" />
      <stop offset="50%" stop-color="#FF0080" />
      <stop offset="100%" stop-color="#FF6A00" />
    </linearGradient>
    <filter id="glow" x="-20%" y="-20%" width="140%" height="140%">
      <feGaussianBlur stdDeviation="8" result="blur" />
      <feMerge>
        <feMergeNode in="blur" />
        <feMergeNode in="SourceGraphic" />
      </feMerge>
    </filter>
  </defs>
  <style>
    @keyframes blink {
      0%, 100% { opacity: 1; filter: drop-shadow(0 0 15px #FF6A00); }
      50% { opacity: 0.4; filter: drop-shadow(0 0 2px #FF0080); }
    }
    @keyframes move {
      0% { transform: translateX(0px); }
      50% { transform: translateX(8px); }
      100% { transform: translateX(0px); }
    }
    .animated-text {
      font-family: 'Segoe UI', system-ui, -apple-system, sans-serif;
      font-weight: 900;
      font-size: 52px;
      fill: url(#glowGrad);
      animation: blink 2s infinite ease-in-out, move 4s infinite ease-in-out;
    }
    .sub-text {
      font-family: 'Segoe UI', system-ui, -apple-system, sans-serif;
      font-weight: 600;
      font-size: 16px;
      fill: #A0AEC0;
      letter-spacing: 5px;
    }
  </style>
  <rect width="100%" height="100%" rx="12" fill="#0D0E15" />
  <g transform="translate(40, 20)">
    <!-- Animated Flame Icon -->
    <g transform="translate(0, 10)">
      <text font-size="36" y="36">🔥</text>
    </g>
    <!-- Text group -->
    <text x="65" y="42" class="animated-text">🔥 FIREBOX BOT</text>
    <text x="68" y="70" class="sub-text">THE ULTIMATE WHATSAPP POWER-BOT</text>
  </g>
</svg>
